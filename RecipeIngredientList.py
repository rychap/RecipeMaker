class RecipeIngredientList:
	def __init__(self, recipes):
		self.recipes = recipes

	def getIngredientIDs():
		recipeIDs = []
		for recipe in self.recipes:
			recipeIDs.append(recipe.id)
		return recipeIDs
